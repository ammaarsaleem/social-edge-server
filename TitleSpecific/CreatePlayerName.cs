using Newtonsoft.Json;
using System.Collections.Generic;
namespace SocialEdge.Title{
    public static class Player
    {
        public static string CreatePlayerName(List<string> namesJson)
        { 
            string name = string.Empty;
            // List<string> names = JsonConvert.DeserializeObject<List<string>>(namesJson);

            return name;
        }
    }
}